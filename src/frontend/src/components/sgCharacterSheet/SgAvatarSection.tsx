import {Avatar} from "@mui/material";
import * as React from "react";

export default function SgAvatarSection()
{
    return (
        <Avatar variant="square" sx={{
            width: '100%',
            height: '100%',
            aspectRatio: '1',
        }}>
            Avatar Placeholder
        </Avatar>
    );
}